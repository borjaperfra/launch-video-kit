# Endpoint TTS compatible con OpenAI (POST /audio/speech).
# Por defecto apunta a Helmcode, que sirve Kokoro. Cambia la URL y el modelo de
# voiceover.json si usas otro proveedor.
TTS_BASE_URL=https://api.helmcode.com/v1
TTS_API_KEY=
